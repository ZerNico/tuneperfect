{
  "name": "tuneperfect",
  "scripts": {
    "dev": "turbo run dev --parallel --force",
    "build": "turbo run build"
  },
  "devDependencies": {
    "turbo": "^2.5.8"
  },
  "dependencies": {
    "@biomejs/biome": "2.0.6"
  },
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "packageManager": "bun@1.3.1"
}
